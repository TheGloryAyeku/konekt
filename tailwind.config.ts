import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        konekt: {
          primary: "#2055bc",
          light: "#f6f2d3",
          background: "#16181c",
        },
      },
    },
  },
};

export default config;
